<component name="DependencyValidationManager">
    <scope name="{{ idea.scopes['build.gradle'].name|default('Gradle build scripts') }}"
           pattern="{{ idea.scopes['build.gradle'].pattern|default('file:build.gradle') }}"/>
</component>
